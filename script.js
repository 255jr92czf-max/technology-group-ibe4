const menuBtn=document.querySelector('.menu-btn');const nav=document.querySelector('.main-nav');if(menuBtn&&nav){menuBtn.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuBtn.setAttribute('aria-expanded',open?'true':'false')});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')))}
const langBtn=document.getElementById('langBtn');let lang='es';function setLang(next){lang=next;document.documentElement.lang=lang;document.querySelectorAll('[data-es][data-en]').forEach(el=>{el.textContent=el.dataset[lang]});langBtn.textContent=lang==='es'?'EN':'ES';localStorage.setItem('ibe4-lang',lang)}if(langBtn){langBtn.addEventListener('click',()=>setLang(lang==='es'?'en':'es'));const saved=localStorage.getItem('ibe4-lang');if(saved==='en')setLang('en')}
const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
const form=document.getElementById('contactForm');const note=document.getElementById('formNote');if(form){form.addEventListener('submit',e=>{e.preventDefault();note.textContent=lang==='es'?'Formulario de demostración: falta conectar el correo corporativo.':'Demo form: corporate email connection is still pending.'})}